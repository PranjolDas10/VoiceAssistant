# ARIA Voice Assistant — Python Dependencies
# Install with: pip install -r requirements.txt
#
# Note: PyAudio requires a C compiler on Windows. Install via:
#   pip install pipwin && pipwin install pyaudio
# Or download the wheel from https://www.lfd.uci.edu/~gohlke/pythonlibs/#pyaudio

# ── Speech I/O ────────────────────────────────────────────────────────────────
SpeechRecognition>=3.10.0
pyttsx3>=2.90
gTTS>=2.3.2
playsound==1.2.2          # 1.3.0 has Windows compatibility issues
PyAudio>=0.2.13

# ── NLP / Knowledge ───────────────────────────────────────────────────────────
wikipedia>=1.4.0
wolframalpha>=5.0.0
deep-translator>=1.11.4   # multi-provider translation (20+ languages)

# ── HTTP / APIs ───────────────────────────────────────────────────────────────
requests>=2.31.0

# ── Google APIs (OAuth 2.0 + Calendar + Gmail) ────────────────────────────────
google-api-python-client>=2.100.0
google-auth-httplib2>=0.1.1
google-auth-oauthlib>=1.1.0

# ── Communication ─────────────────────────────────────────────────────────────
twilio>=8.5.0

# ── Browser Automation (pizza ordering / web scraping) ────────────────────────
selenium>=4.15.0

# ── System / Utilities ────────────────────────────────────────────────────────
python-dotenv>=1.0.0
pyjokes>=0.6.0
pyautogui>=0.9.54         # screenshot, volume keys
Pillow>=10.0.0            # required by pyautogui

# ── Windows-only ─────────────────────────────────────────────────────────────
winshell>=0.6;platform_system=="Windows"
pywin32>=306;platform_system=="Windows"
